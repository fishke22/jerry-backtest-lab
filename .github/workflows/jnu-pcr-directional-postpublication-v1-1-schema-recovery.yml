name: JNU PCR Directional V1.1 Schema Recovery

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  replicate:
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Download authoritative PCR information panel
        uses: actions/download-artifact@v4
        with:
          name: jnu-pcr-full-information-panel-recovery-2023-02-v1
          path: pcr_authoritative
          repository: fishke22/jerry-backtest-lab
          run-id: "33611084116"
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Run frozen v1.1 through schema adapter
        run: >
          python scripts/run_jnu_pcr_directional_postpublication_v1_1_schema_adapter.py
          --panel pcr_authoritative/pcr_results/jnu_pcr_full_information_panel_v1.json
          --prereg config/jnu_pcr_directional_postpublication_prereg_v1_1.json
      - name: Upload v1.1 directional replication evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jnu-pcr-directional-postpublication-v1-1-schema-recovery
          path: |
            results/jnu_pcr_directional_postpublication_v1_1.json
            reports/jnu_pcr_directional_postpublication_v1_1.md
